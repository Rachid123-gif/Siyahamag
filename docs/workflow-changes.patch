diff --git a/.github/workflows/daily-seo.yml b/.github/workflows/daily-seo.yml
index 5f223de..46c6205 100644
--- a/.github/workflows/daily-seo.yml
+++ b/.github/workflows/daily-seo.yml
@@ -34,6 +34,10 @@ jobs:
         run: npm ci --legacy-peer-deps || npm install --legacy-peer-deps
 
       - name: Run daily SEO script
+        env:
+          # When set, articles are generated ORIGINAL via the Claude API.
+          # When absent, the script falls back to thin (noindex) summaries.
+          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
         run: |
           set -e
           node scripts/daily-seo.mjs | tee seo-output.log
@@ -83,12 +87,15 @@ jobs:
           sleep 60
           node scripts/indexnow.mjs
 
-      - name: Ping Google about main sitemap
+      - name: Submit URLs to Google Indexing API
+        # Notifies Google directly (faster than waiting for a crawl). No-op if
+        # GOOGLE_INDEXING_SA_JSON isn't configured. Replaces the deprecated
+        # sitemap "ping" endpoint, which Google removed in 2023.
         if: steps.check.outputs.has_changes == 'true'
-        run: |
-          curl -s "https://www.google.com/ping?sitemap=https://siyahamag.ma/sitemap.xml" || true
-          curl -s "https://www.google.com/ping?sitemap=https://siyahamag.ma/news-sitemap.xml" || true
-          echo "Google pinged for both sitemaps."
+        env:
+          URLS: ${{ steps.urls.outputs.urls }}
+          GOOGLE_INDEXING_SA_JSON: ${{ secrets.GOOGLE_INDEXING_SA_JSON }}
+        run: node scripts/google-index.mjs
 
       - name: Upload log artifact
         if: always()
diff --git a/.github/workflows/health-check.yml b/.github/workflows/health-check.yml
index 0c17fab..32d9be9 100644
--- a/.github/workflows/health-check.yml
+++ b/.github/workflows/health-check.yml
@@ -73,10 +73,9 @@ jobs:
             exit 1
           fi
 
-      - name: Ping Google about sitemap
-        run: |
-          curl -s "https://www.google.com/ping?sitemap=https://siyahamag.ma/sitemap.xml" || true
-          echo "Google notified of sitemap"
+      # NOTE: the Google sitemap "ping" endpoint was removed by Google in 2023
+      # and is now a no-op — discovery happens via Search Console + the
+      # Indexing API (see scripts/google-index.mjs). Step intentionally dropped.
 
       - name: Create issue on failure
         if: failure()
